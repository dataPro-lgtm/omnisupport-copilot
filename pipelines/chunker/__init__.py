"""Week07 chunking adapters aligned with the course deck.

The runtime pipeline still enters through ``pipelines.parse_normalize.chunking``.
This package exposes the named strategies used in the Week07 lesson so learners
can map the classroom concepts to stable code paths.
"""
